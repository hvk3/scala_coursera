package week3

object scratch {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(65); 
	val x = new week3.Rational(1, 2);System.out.println("""x  : week3.Rational = """ + $show(x ));$skip(22); 
	println("hey there")}
}
