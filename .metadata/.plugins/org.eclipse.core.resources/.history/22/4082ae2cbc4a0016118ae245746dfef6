package week3

object rationals {
	def gcd(a: Int, b: Int) = 
	if (!b) a
	
  val x = new Rational(1, 2)                      //> x  : week3.Rational = week3.Rational@deb6432
  x.numer                                         //> res0: Int = 1
}

class Rational(x: Int, y: Int){
  	def numer = x
  	def denom = y
  }