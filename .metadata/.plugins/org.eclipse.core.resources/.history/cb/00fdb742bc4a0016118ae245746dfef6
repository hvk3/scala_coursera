package week3

object rationals {
	def gcd(a: Int, b: Int): Int =
		if (b == 0) a
		else gcd(b, a % b)
	val x = new Rational(1, 2)
	x.numer
}

class Rational(x: Int, y: Int) {
	def numer = x
	def denom = y
}